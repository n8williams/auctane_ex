defmodule Auctane.ShipEngineData.Carriers.CarrierServiceTest do
  use ExUnit.Case
  alias Auctane.ShipEngineData.Carriers.CarrierService
  doctest CarrierService

  test "greets the world" do
    # assert ShipEngineData.hello() == :world
  end
end
